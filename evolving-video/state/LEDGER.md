# LEDGER — every piece, forever (newest at bottom; never delete, only append)

Format per entry: id - date - title - concept - orientation/dur - assets(+license) -
audio - techniques(stretch in **bold**) - scores(7 dims -> composite) - wins - lessons.

(No entries yet — your first cycle starts the history.)
